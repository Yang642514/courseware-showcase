## 项目定位

这是晴天的课程课件与临时网页展示工作台。

第一阶段目标不是做复杂后台，而是快速发布可访问的静态内容：HTML PPT、课程说明、作业模板、客户演示网页、临时项目页面。

## 第一性原则

- 先服务交付，不做展示炫技。
- 优先让客户和学员能顺利打开、看懂、使用。
- 内容组织比技术复杂度更重要。
- 先跑通静态发布流程，再考虑权限、后台、上传、访问统计。

## 目录约定

- `docs/`：项目规则、设计文档、实施计划、部署说明。
- `src/`：Astro 应用源码。
- `src/content/`：课程、课件、临时项目的配置清单。
- `src/pages/`：页面路由。
- `public/decks/`：HTML PPT 静态资源，每个课件一个独立目录。
- `public/resources/`：可下载或可访问的作业模板、资料、附件。
- `public/demos/`：临时网页 demo 或客户演示页面。

## 命名约定

- 课程路径用 `course-slug`，例如 `enterprise-ai-training`。
- 课件路径用 `lesson-N`，例如 `lesson-1`。
- 临时项目路径用简短英文 slug，例如 `client-demo-202605`。
- 文件夹名只用英文、小写、数字和连字符，避免线上 URL 出现中文路径。

## 内容发布原则

- 首页只展示可公开内容。
- 半私密内容可以不出现在首页，只通过直达链接访问。
- 不上传客户隐私、内部数据、手机号、微信号、合同、真实订单明细。
- 如需展示真实截图，先脱敏再放入 `public/`。

## 开发与验证

- 改完必须跑 `npm run build`。
- 涉及页面视觉，必须本地预览检查主要页面。
- 不为了通过构建而删除报错内容，先找根因。
- 密钥、token、kubeconfig 不进入代码仓库。

## 部署策略

- 第一版沿用个人网站的模式：Astro 静态构建、Docker Nginx 托管、GitHub Actions 构建镜像、Sealos 滚动更新。
- 部署配置和密钥只放在 GitHub Secrets 或 Sealos 环境中。
- 不自动执行 `git push`，等明确指令。

